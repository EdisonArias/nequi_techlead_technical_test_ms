server:
  port: 8080
spring:
  application:
    name: "nequi_techlead_technical_test_ms"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  data:
    mongodb:
      uri: "mongodb://admin:SuperSecreta123@localhost:27017/technicalTest?authSource=admin&directConnection=true&serverSelectionTimeoutMS=2000"
aws:
  dynamodb:
    endpoint: "http://localhost:8000"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
